'use client';

import React, { FC } from 'react';
import { LearningProps } from '.';

interface BasicLearningProps extends LearningProps {
}

const BasicLearning: FC<BasicLearningProps> = ({}) => {
    return (    
        <div>
            
        </div>
    );
};

export default BasicLearning;