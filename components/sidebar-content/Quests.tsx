'use client';

import React, { FC } from 'react';

interface QuestsProps {
}

const Quests: FC<QuestsProps> = ({}) => {
    return (
        <div>
            
        </div>
    );
};

export default Quests;